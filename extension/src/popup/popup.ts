// F01 scaffold stub — trạng thái đăng nhập + settings đến ở F06/F08.
document.addEventListener("DOMContentLoaded", () => {
  const el = document.getElementById("status");
  if (el) el.textContent = "Deutsch Lernen — scaffold (F01)";
});
